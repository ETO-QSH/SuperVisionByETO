# SuperVisionByETO 项目使用文档

## 目录

- [SuperVisionByETO 项目使用文档](#supervisionbyeto-项目使用文档)
  - [目录](#目录)
  - [项目结构](#项目结构)
  - [功能描述](#功能描述)
    - [导航栏](#导航栏)
    - [主页](#主页)
    - [管理](#管理)
      - [功能详解](#功能详解)
    - [设置](#设置)
    - [文档](#文档)
    - [源码](#源码)
  - [命令行工具](#命令行工具-yolobyetoexe)
  - [详细技术说明](#详细技术说明)


---

## 项目结构

软件的初始项目结构如下：

```
SuperVisionByETO/
├── bin/
│   └── YoloByETO.exe         # 命令行工具
├── model/
│   └── yolov8s.pt            # 预置的YOLO模型文件
├── SuperVisionByETO.exe      # 主程序
```

双击主程序 `SuperVisionByETO.exe` 之后将会打开 GUI，并在根目录下创建 `config` 文件夹，编写 `config.json` 文件，用于保留用户配置。同时会清空输出目录 `output` ，清空数据残留。

## 功能描述

### 导航栏

- **标题**：预留的标题页，建议改成模型训练页。
- **管理**：核心功能区，用于文件管理和标注。
- **设置**：配置管理，包括模型选择和目录管理。
- **文档**：查看软件使用文档。
- **源码**：访问软件的源代码仓库。

### 主页

预留的标题页，目前没有具体功能。建议将其改为模型训练页面。

### 管理

![管理](attachment:image2.png)

这是软件的核心部分，用户可以在这里添加图片文件（支持 `.png` 和 `.jpg` 格式），并通过拖放文件到指定区域进行管理。页面提供了三个主要按钮：

- **清空**：清空当前列表中的所有文件。
- **开始标注**：开始对选中的图片进行标注处理。
- **保存**：将处理后的图片保存为 ZIP 文件。

#### 功能详解

- **添加图片文件**：用户可以通过点击“打开”按钮，选择本地的图片文件进行添加，或者直接将图片文件拖放到指定区域，均可以批量操作。
- **文件列表**：添加的图片文件会显示在文件列表中，用户可以通过点击文件名来预览标注后的文件（自动定位），右键预览区域会按照原图分辨率显示。
- **标注处理**：点击“开始标注”按钮，软件将使用选定的模型对列表中的图片进行标注处理。处理过程中，按钮状态会变为“处理中...”，并禁用其他按钮。
- **保存结果**：处理完成后，用户可以点击“保存”按钮，将标注后的图片保存为 ZIP 文件。用户可以通过文件对话框选择保存路径和文件名。

### 设置

在设置页面，用户可以更改模型目录和保存目录，选择标注用的模型，以及下载官方标注模型。具体功能包括：

- **模型选择**：从下拉菜单中选择标注用的模型。
- **模型下载**：下载官方提供的标注模型。
- **目录管理**：更改模型目录和保存目录。

### 文档

提供软件的使用文档，帮助用户更好地理解和使用软件，目前为本文档图片版。

### 源码

链接到软件的源代码仓库，用户可以访问并查看源代码。

## 命令行工具 YoloByETO.exe

`YoloByETO.exe` 是一个命令行工具，用于执行 YOLO 模型的检测任务并保存结果图。以下是其使用方法：

```bash
YoloByETO.exe -m <model_path> -i <input_path> -o <output_path>
```

- `-m`, `--model`：指定 YOLO 模型的路径（*.pt）。
- `-i`, `--input`：指定输入图片的路径。
- `-o`, `--output`：指定输出图片的保存路径。

这个工具可以在没有 GUI 的情况下，通过命令行快速批量执行模型检测任务。

## 详细技术说明

请浏览[deepwiki文档](https://deepwiki.com/ETO-QSH/SuperVisionByETO/)，可自行获取最新更新以及询问AI了解更多细节。

---

###### 本项目采用 GNU Affero General Public License v3.0 协议
